<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.bestdata-chi</groupId>
        <artifactId>ts-oss-parent</artifactId>
        <!--        <version>1.0.0-SNAPSHOT</version>-->
        <version>1.0.3</version>
    </parent>

    <artifactId>ts-oss-tdengine</artifactId>
    <name>ts-oss-tdengine</name>
    <description>TDengine 完整实现（基于 taos-jdbcdriver 的 JDBC 抽象）</description>

    <dependencies>
        <dependency>
            <groupId>io.github.bestdata-chi</groupId>
            <artifactId>ts-oss-core</artifactId>
        </dependency>
        <!-- TDengine JDBC 驱动，版本由父 POM 统一管理 -->
        <dependency>
            <groupId>com.taosdata.jdbc</groupId>
            <artifactId>taos-jdbcdriver</artifactId>
        </dependency>
        <!-- 连接池（HikariCP），版本由父 POM 统一管理 -->
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
        </dependency>
    </dependencies>
</project>